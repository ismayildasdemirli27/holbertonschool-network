#!/usr/bin/env bash
# Configures /etc/hosts to make localhost resolve to 127.0.0.2 and facebook.com to 8.8.8.8
sudo sed -i '/localhost/c\127.0.0.2 localhost' /etc/hosts
echo "8.8.8.8 facebook.com" >> /etc/hosts
